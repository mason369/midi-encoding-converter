name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.1.0)'
        required: true
        default: 'v1.1.0'

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller PyQt6 chardet

    - name: Build executable
      run: |
        pyinstaller --clean --noconfirm `
          --name "MIDI_Encoder" `
          --onefile `
          --windowed `
          --hidden-import PyQt6 `
          --hidden-import PyQt6.QtCore `
          --hidden-import PyQt6.QtGui `
          --hidden-import PyQt6.QtWidgets `
          --hidden-import chardet `
          midi_converter_standalone.py

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: MIDI_Encoder
        path: dist/MIDI_Encoder.exe

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v4
      with:
        name: MIDI_Encoder

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: MIDI_Encoder.exe
        generate_release_notes: true
        body: |
          ## MIDI 编码转换器 / MIDI Encoding Converter

          ### 下载 / Download

          下载 `MIDI_Encoder.exe` 即可直接运行，无需安装 Python。

          Download `MIDI_Encoder.exe` and run directly - no Python installation required.

          ### 更新内容 / What's New

          - 🔍 自动检测源文件编码 / Auto-detect source encoding
          - 🌐 中英文界面切换 / Bilingual interface
          - 🎨 改进的用户界面 / Improved UI

      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
